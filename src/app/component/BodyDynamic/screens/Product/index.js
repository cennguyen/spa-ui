import React, { memo } from "react";

const Product = () => {

    return (
        <React.Fragment>
            <h1>
                This is Product Page
            </h1>
        </React.Fragment>)
}
export default memo(Product);